<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.key.win.system.dao.SysRolePermissionDao">


    <delete id="deleteBySelective">
        delete from sys_role_permission
        <where>
            <if test="roleId != null">
                and role_id = #{roleId}
            </if>
            <if test="permissionId != null">
                and permission_id = #{permissionId}
            </if>
        </where>
    </delete>

    <select id="findByRoleIds" resultType="com.key.win.common.model.system.SysPermission">
        select distinct p.* from
        sys_permission p
        inner join
        sys_role_permission rp on
        p.id = rp.permission_id
        <where>
            rp.enable_flag = 1
            and p.enable_flag = 1
            and rp.role_id in
            <foreach collection="roleIds" item="roleId" open="("
                     separator="," close=")">
                #{roleId}
            </foreach>
        </where>
    </select>
    <insert id="saveBatchRoleIdAndPermissions">
        INSERT INTO
        sys_role_permission(id,create_date,create_user_id,create_user_name,enable_flag,update_date,update_user_id,update_user_name,version,role_id,permission_id)
        VALUES
        <foreach collection="permissions" item="permissionId"
                 separator=",">
            ('${@com.key.win.base.util.DefaultIdentifierGeneratorUtils@getGeneratorLongId()}',current_timestamp(),'${@com.key.win.base.auth.AuthenticationUtil@getUserId()}','${@com.key.win.base.auth.AuthenticationUtil@getUserName()}',1,null,null,null,0,#{roleId},#{permissionId})
        </foreach>
    </insert>

    <insert id="saveBatchRoleIdsAndPermissionId">
        INSERT INTO
        sys_role_permission(id,create_date,create_user_id,create_user_name,enable_flag,update_date,update_user_id,update_user_name,version,role_id,permission_id)
        VALUES
        <foreach collection="roleIds" item="roleId"
                 separator=",">
            ('${@com.key.win.base.util.DefaultIdentifierGeneratorUtils@getGeneratorLongId()}',current_timestamp(),'${@com.key.win.base.auth.AuthenticationUtil@getUserId()}','${@com.key.win.base.auth.AuthenticationUtil@getUserName()}',1,null,null,null,0,#{roleId},#{permissionId})
        </foreach>
    </insert>
</mapper>